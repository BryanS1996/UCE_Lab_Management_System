name: ▶ Nightly Apply — QA

on:
  schedule:
    # Runs at 12:00 UTC (07:00 ECT) every day — re-creates the QA environment for the workday
    - cron: '0 12 * * *'
  # Allow manual trigger to spin up QA on demand
  workflow_dispatch:

env:
  AWS_REGION: us-east-1

jobs:
  terraform-apply-qa:
    name: Terraform Apply — QA
    runs-on: ubuntu-latest
    environment: qa

    steps:
      - uses: actions/checkout@v4
        with:
          ref: qa

      - uses: hashicorp/setup-terraform@v3
        with:
          terraform_version: '1.5.0'

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.QA_AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.QA_AWS_SECRET_ACCESS_KEY }}
          aws-session-token: ${{ secrets.QA_AWS_SESSION_TOKEN }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Verify backend variables
        run: |
          if [ -z "${{ vars.TF_STATE_BUCKET }}" ] || [ -z "${{ vars.TF_LOCK_TABLE }}" ]; then
            echo "::error::Missing TF_STATE_BUCKET or TF_LOCK_TABLE in the QA Environment."
            exit 1
          fi

      - name: Terraform Init (S3 backend)
        working-directory: infra/terraform/environments/qa
        env:
          TF_STATE_BUCKET: ${{ vars.TF_STATE_BUCKET }}
          TF_LOCK_TABLE: ${{ vars.TF_LOCK_TABLE }}
          TF_AWS_REGION: ${{ vars.TF_AWS_REGION }}
        run: bash ../../scripts/init-backend.sh qa

      - name: Terraform Apply — QA only
        working-directory: infra/terraform/environments/qa
        run: |
          terraform apply -auto-approve \
            -var="enable_rds=false" \
            -var="db_password=${{ secrets.DB_PASSWORD }}"

      - name: Show QA outputs (Bastion IP, ALB DNS, etc.)
        working-directory: infra/terraform/environments/qa
        run: terraform output

      - name: Notify apply completed
        run: echo "✅ QA infrastructure re-created at $(date -u '+%Y-%m-%d %H:%M UTC')"
