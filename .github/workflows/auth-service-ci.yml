name: Auth Service — CI

on:
  push:
    paths:
      - 'services/auth-service/**'
      - '.github/workflows/_service-ci.yml'
  pull_request:
    branches: [ "qa" ]
    paths:
      - 'services/auth-service/**'
      - '.github/workflows/_service-ci.yml'
  workflow_dispatch:

jobs:
  lint-and-test:
    uses: ./.github/workflows/_service-ci.yml
    secrets: inherit
    with:
      service-name: auth
      service-dir: services/auth-service
      coverage-artifact-name: auth-coverage
