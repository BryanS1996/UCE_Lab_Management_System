name: Laboratory Service — CI

on:
  push:
    branches: [main, qa, feature/*]
    paths:
      - 'services/laboratory-service/**'
      - '.github/workflows/laboratory-service-ci.yml'
  pull_request:
    branches: [main, qa]
    paths:
      - 'services/laboratory-service/**'

jobs:
  lint-and-test:
    name: Lint & Tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: services/laboratory-service/package-lock.json
      - name: Install
        working-directory: services/laboratory-service
        run: npm ci --legacy-peer-deps
      - name: Build
        working-directory: services/laboratory-service
        run: npm run build
      - name: Test
        working-directory: services/laboratory-service
        run: npm run test -- --forceExit
